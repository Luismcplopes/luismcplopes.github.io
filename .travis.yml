sudo: true
dist: trusty
language: html

language: python
python:
  - 2.7
  
notifications: 
  email: true
  slack: ${GH_TOKEN}
    on_success: always # default: change 
    on_failure: always # default: always  

notifications: 
  email: true
  slack:
    rooms:
      - ${GH_TOKEN}
    on_success: change # default: always
    on_failure: always # default: always  
